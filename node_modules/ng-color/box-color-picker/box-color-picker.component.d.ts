import { OnInit } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { ColorOutput } from '../shared/color-utility/color-output';
import { ColorUtilityService } from '../shared/color-utility/color-utility.service';
import { SaturationLightness } from '../shared/hsl/saturation-lightness';
export declare class BoxColorPickerComponent implements ControlValueAccessor, OnInit {
    private colorUtility;
    startHex: string;
    hue: number;
    saturationLightness: SaturationLightness;
    private onTouchedCallback;
    private onChangeCallback;
    constructor(colorUtility: ColorUtilityService);
    ngOnInit(): void;
    calculateColor(): void;
    writeValue(obj: ColorOutput): void;
    registerOnChange(fn: (_: ColorOutput) => void): void;
    registerOnTouched(fn: () => void): void;
}
