import { ElementRef } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { MouseHandlerOutput } from '../../shared/mouse-handler/mouse-handler-output';
import { Vector } from '../../vector';
export declare class BoxHueComponent implements ControlValueAccessor {
    private el;
    cursorPosition: Vector;
    private onTouchedCallback;
    private onChangeCallback;
    constructor(el: ElementRef);
    writeValue(hue: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    setHue(mouseEvent: MouseHandlerOutput): void;
}
