import { ElementRef } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { Vector } from '../../vector';
import { ColorUtilityService } from '../color-utility/color-utility.service';
import { MouseHandlerOutput } from '../mouse-handler/mouse-handler-output';
export interface SaturationLightness {
    saturation: number;
    lightness: number;
}
export declare class HslBaseComponent implements ControlValueAccessor {
    private el;
    private readonly colorService;
    cursorPosition: Vector;
    hue: number;
    private saturation;
    private lightness;
    private onTouchedCallback;
    private onChangeCallback;
    constructor(el: ElementRef, colorService: ColorUtilityService);
    setSaturationLightness(mouseEvent: MouseHandlerOutput): void;
    hsl(): string;
    value: SaturationLightness;
    writeValue(v: SaturationLightness): void;
    registerOnChange(fn: (_: SaturationLightness) => void): void;
    registerOnTouched(fn: () => void): void;
}
