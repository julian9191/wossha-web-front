import { ColorOutput } from './color-output';
import { Hsl } from './hsl';
export declare class ColorUtilityService {
    constructor();
    createColorOutput(hue: number, saturation: number, lightness: number): ColorOutput;
    calculateHslFromHex(v: string): Hsl;
    calculateHexFromHsl(hsl: Hsl): string;
}
